package io.github.pingpongboss.explodedlayers.samples.common.utils

/**
 * Returns the current time in milliseconds since the Unix epoch. This is an expect function,
 * requiring a platform-specific implementation.
 */
expect fun currentTimeMillis(): Long
